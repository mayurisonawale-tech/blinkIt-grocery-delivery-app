export const environment = {
  production: true,
  // Deployed API base URL. Must end with a trailing slash.
  // Replace this with your own API URL after deploying the backend.
  apiUrl: 'https://blinkit-clone-api.onrender.com/'
};
